#%PAM-1.0
# l2kdm -- the Windows 2000 logon screen (Alpine: the base-* stacks)
auth      requisite  pam_nologin.so
auth      include    base-auth
account   include    base-account
password  include    base-password
session   include    base-session
